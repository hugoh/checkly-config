---
name: release

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: hugoh/gh-workflows/.github/workflows/release.yml@d8b9c2df31a2df9ae138f25ec02633a3427c59a2 # v1.33.2
    permissions:
      contents: write
